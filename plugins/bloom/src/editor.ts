/**
 * @forge/plugin-bloom (Editor)
 * 暴露给 @forge/editor 的逻辑与 UI
 */

export const BloomEditorPlugin = {
  name: 'Forge_Bloom_Editor',
  onInstall(editor: any) {
    console.log("Bloom Editor Plugin Installed");
  }
};
